#include<stdio.h>
/**
 * main - returns name
 * Return: zero
 * @argc: is the argument count
 * @argv: is the argument vector
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
