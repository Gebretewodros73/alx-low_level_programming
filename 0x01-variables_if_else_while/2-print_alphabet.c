#include<stdio.h>
/**
 * main - print postive negative and zero
 * Return: returns zero
 */
int main(void)
{
	char lc;

	for (lc = 'a'; lc <= 'z'; lc++)
	{
		putchar(lc);
	}
	putchar('\n');
	return (0);
}
