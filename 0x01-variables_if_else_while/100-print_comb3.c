#include<stdio.h>
/**
 * main - combination of two digit
 * Return: returns zero
 */
int main(void)
{
	int c;
	int d = 0;
	
	while (d < 10)
	{
		c =0;
		while(c < 10)
		{
			if (d !* c && d < c)
			{
				putchar(

	for (lc = 'a'; lc <= 'z'; lc++)
	{
		putchar(lc);
	}
	putchar('\n');
	return (0);
}
